# LinguaBee - pełne zależności runtime

# GPU (CUDA) - zostaw, jeśli używasz GPU. Dla CPU możesz usunąć tę linię.
--extra-index-url https://download.pytorch.org/whl/cu128
torch

# GUI
PySide6

# Core HTTP / API
httpx

# Download / media input
yt-dlp
av

# Audio processing
librosa
soundfile
pydub
noisereduce

# ASR / alignment / diarization
faster-whisper
whisperx
pyannote.audio

# Translation / summarization models
transformers
ctranslate2>=4.7.1
sentencepiece
sacremoses
huggingface-hub

# Document outputs
python-docx

# ---------------------------------------------------------------------------
# Opcjonalne backendy lokalne (zainstaluj ręcznie na docelowej platformie)
# ---------------------------------------------------------------------------

# Apple Silicon (Mac M1/M2/M3/M4) — MLX backend
# pip install mlx-lm
# mlx-lm

# NVIDIA GPU (np. RTX 3090 Ti) — llama-cpp-python z obsługą CUDA
# WAŻNE: wymagana kompilacja z flagą CUDA, NIE zwykłe pip install
# CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --force-reinstall --no-cache-dir
# llama-cpp-python
